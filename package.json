{
  "name": "arc-mcp-server",
  "version": "2.0.0",
  "description": "MCP server for Arc browser automation with Playwright-style semantic locators",
  "author": "Andrew Gazelka",
  "type": "module",
  "private": false,
  "bin": {
    "arc-mcp-server": "./index.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @arc-mcp/applescript && npm run build -w @arc-mcp/browser && npm run build -w arc-mcp-server",
    "typecheck": "npm run typecheck -w @arc-mcp/applescript && npm run typecheck -w @arc-mcp/browser && npm run typecheck -w arc-mcp-server",
    "test": "vitest run",
    "test:watch": "vitest",
    "pack": "npm run build && node scripts/pack.mjs",
    "ci": "npm run build && npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "esbuild": "^0.27.0",
    "typescript": "^5.7.2",
    "vitest": "^4.0.15"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.24.2"
  }
}
